node_modules/
.env
.DS_Store
menubar/PropHunt.app/Contents/MacOS/PropHunt
menubar/prophunt-agent

# Datos locales del usuario (no sobreescribir con updates)
data/contacted.json
data/logs/

# Config local (cada equipo tiene su propia config)
config.json

# Local Netlify folder
.netlify

# Build output (distributed via GitHub Releases, not in repo)
dist/
